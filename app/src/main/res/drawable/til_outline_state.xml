<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressionado: fundo cinza fraco + borda roxa -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <corners android:radius="12dp" />
            <solid android:color="#F2F2F4" />
            <stroke android:width="2dp" android:color="@color/pomotiva_secondary" />
        </shape>
    </item>

    <!-- Focado: fundo cinza fraco + borda roxa -->
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <corners android:radius="12dp" />
            <solid android:color="#F2F2F4" />
            <stroke android:width="2dp" android:color="@color/pomotiva_secondary" />
        </shape>
    </item>

    <!-- Padrão: fundo transparente + borda roxa -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="12dp" />
            <solid android:color="@android:color/transparent" />
            <stroke android:width="1dp" android:color="@color/pomotiva_secondary" />
        </shape>
    </item>
</selector>

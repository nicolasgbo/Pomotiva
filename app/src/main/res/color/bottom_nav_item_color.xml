<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selecionado -->
    <item android:state_checked="true" android:color="@color/pomotiva_secondary"/>
    <!-- Pressionado (durante o toque) -->
    <item android:state_pressed="true" android:color="@color/pomotiva_secondary"/>
    <!-- Padrão (não selecionado) -->
    <item android:color="#8A000000"/>
</selector>

<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AndroidAppTemplate" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorSurface</item>
        <!-- Altura levemente maior da ActionBar/Toolbar -->
        <item name="actionBarSize">64dp</item>
        <!-- Estilo global para TextInputLayout -->
        <item name="textInputStyle">@style/Widget.Pomotiva.TextInputLayout.OutlinedBox</item>
        <!-- Tema dos diálogos Material (padrão) -->
        <item name="materialAlertDialogTheme">@style/ThemeOverlay.MaterialComponents.MaterialAlertDialog</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Tema sem ActionBar para telas que não precisam de barra superior (ex.: Login) -->
    <style name="Theme.AndroidAppTemplate.NoActionBar" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Herdar mesmas cores base do tema principal -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:statusBarColor">?attr/colorSurface</item>
        <!-- Manter estilos globais utilizados no app -->
        <item name="textInputStyle">@style/Widget.Pomotiva.TextInputLayout.OutlinedBox</item>
        <item name="materialAlertDialogTheme">@style/ThemeOverlay.MaterialComponents.MaterialAlertDialog</item>
    </style>
</resources>